const express = require('express');
const { markAttendanceByQR } = require('../controllers/attendanceController');

const router = express.Router();

router.post('/markByQR', markAttendanceByQR);

module.exports = router;